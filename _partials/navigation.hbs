<div class="nav">
  <h3 class="nav-title">Menu</h3>
  <a href="#" class="nav-close">
    <span class="hidden">Close</span>
  </a>
  <ul>
    {{#each @navigation}}
      <li {{#if current}}class="nav-current"{{/if}} role="presentation"><a href="{{fullpath path}}">{{title}}</a></li>
    {{/each}}
  </ul>
  <a class="subscribe-button icon-feed" href="{{fullpath '/rss/'}}">Subscribe</a>
</div>
<span class="nav-cover"></span>
